<!-- <?xml version="1.0" encoding="UTF-8"?> -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.zzzpro.zzz.dao.MemberDao">
	
	<select id="idCheck">
		select count(*) from member where m_id=#{m_id}
	</select>
	
	<select id="nickCheck">
		select count(*) from member where m_nick=#{m_nick}
	</select>
	
	<select id="emailCheck">
		select count(*) from member where m_email=#{m_email}
	</select>
	
	<insert id="join">
		insert into member values(#{m_id},#{m_nick},#{m_pw},#{m_email},default,default,default)
	</insert>
	
	<select id="login">
		select m_pw from member where m_id=#{m_id}
	</select>
	
	<select id="getNick">
		select m_nick from member where m_id=#{b_writer}
	</select>
	
	
</mapper>